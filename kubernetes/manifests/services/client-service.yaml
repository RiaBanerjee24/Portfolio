apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: default
spec:
  selector:
    app: client        # routes traffic to pods with this label
  ports:
    - protocol: TCP
      port: 80         # service port
      targetPort: 3000 # container port
  type: ClusterIP      # internal only — ALB will route to this