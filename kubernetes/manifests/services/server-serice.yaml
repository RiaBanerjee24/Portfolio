apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: default
spec:
  selector:
    app: server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5002
  type: ClusterIP