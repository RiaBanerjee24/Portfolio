apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: default
spec:
  selector:
    app: client
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.client.port }}
  type: ClusterIP